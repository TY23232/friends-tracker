services:
  - type: web
    name: friends-tracker
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GOOGLE_MAPS_API_KEY
        sync: false
      - key: CORS_ORIGIN
        value: '*'
